<div id="form-student" class="container-fluid"><div class="row"><div class="col-sm-10 col-sm-offset-1">
<% if notice %>
  <div class="alert alert-success"><%= notice %></div>
<% end %>
<h1 class="page-header">Student Information</h1>
<%= render 'form' %>
<a class="btn btn-primary" href="<%= session[:user]["access"] == 1 || session[:user]["access"] == 2 ? '/students' : '/students-main' %>">
    <span class="glyphicon glyphicon-arrow-left"></span> Back</a>
</div></div></div>
<script>
  $(document).ready(function() {
    $(".form-control").prop("disabled", true);
    $("#div-btn-save").hide();
  });
</script>