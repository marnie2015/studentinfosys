<div class="row text-center">
  <div class="col-sm-10 col-sm-offset-1">
  <p id="notice"><%= notice %></p>
  <p>
    <a href="/events" class="btn btn-sm btn-primary pull-left">Back</a>
    <strong>Description:</strong>
    <%= @event.description %>
  </p>
  <img src="<%= "#{@event.image}" %>" width="80%">
  </div>
</div>
