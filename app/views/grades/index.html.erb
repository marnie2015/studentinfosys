<div class="container-fluid">
    <div class="row">
        <div class="col-sm-10 col-sm-offset-1">
            <p id="notice"><%= notice %></p>
            <h1 class="page-header">Grades</h1>
            <div class="row page-header" style="margin-bottom: 10px;">
              <form>
              <div class="col-sm-3">
               <label>Search by:</label>
                <select class="form-control" name="searchby">
                  <option value="fname">First Name</option>
                  <option value="lname" <%= "selected" if params[:searchby] == "lname" %>>Last Name</option>
                  <option value="year_level" <%= "selected" if params[:searchby] == "year_level" %>>Year Level</option>
                </select>
              </div>
              <div class="col-sm-3">
                <label>Search text:</label>
                <input type="text" class="form-control" name="txt_search" value="<%= params[:txt_search] if params[:txt_search] %>">
              </div>
              <div class="col-sm-2" style="margin-top: 25px;">
                <button class="btn btn-primary" type="submit">
                    <span class="glyphicon glyphicon-search"></span> Search
                </button>
              </div>
              </form>
            </div>
            <table class="table table-bordered table-striped">
              <thead>
                <tr>
                  <th>Student ID</th>
                  <th>First name</th>
                  <th>Middle name</th>
                  <th>Last name</th>
                  <th>Address 1</th>
                  <th>Status</th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% @students.each do |student| %>
                  <tr>
                    <td><%= student.student_id %></td>
                    <td><%= student.fname %></td>
                    <td><%= student.mname %></td>
                    <td><%= student.lname %></td>
                    <td><%= student.address %></td>
                    <td><%= student.status %></td>
                    <td class="text-center"><%= link_to "/grades/show/" + student.id.to_s, :class => "btn btn-primary btn-sm" do %>
                          <span class="glyphicon glyphicon-eye-open"></span> View Grades
                        <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
            <br>
            <div class="row text-center">
              <%= will_paginate @students %>
            </div>
        </div>
    </div>
</div>
